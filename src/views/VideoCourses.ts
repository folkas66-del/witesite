import PublicHeader from "./_PublicHeader";
export default function VideoCourses() {
  return `${PublicHeader("#/video-courses")}
    <section class="container-soft py-10"><h1 class="text-4xl font-bold">Відео курси</h1><p class="mt-3 muted">Каталог курсів (публічна вітрина). Для купівлі потрібна реєстрація.</p></section>`;
}
